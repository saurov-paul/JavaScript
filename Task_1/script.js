// - Create a variable named age and assign it an integer value.
// - Create a variable named name and assign it a string value.
// - Create a variable named isStudent and assign it a boolean value.
// - Create a variable named hobbies and assign it an array containing a few string values (e.g., hobbies).
// - Create an info object with two properties: schoolName (a string) and grade (an integer).

// - Use console.log() to print each variable to the console.

// - Use the typeof operator to check the data type of each variable and print the result using console.log()

let age = 28;
let name = "Saurov";
let isStudent = true;
let hobbies = ["Game", "travelling", "cooking"];
let info = { schooleName: "Business college", grade: 4 };

console.log(age);
console.log(name);
console.log(isStudent);
console.log(hobbies);
console.log(info);

console.log(typeof age);
console.log(typeof name);
console.log(typeof isStudent);
console.log(typeof hobbies);
console.log(typeof info);

/* 
- Create variables a and b, and assign them integer values.
- Use add, subtraction, multiply, divide, and modulus operator for a and b. Store each result in separate variables (sum, difference, product, quotient, reminder).
- Calculate the remainder of a divided by b using the modulus operator and store it in a variable called remainder.

- Use console.log() to print the results of each operation to the console.

- Add comments in your code to explain what each operation does. */

let a = 20;
let b = 70;

let sum = a + b;
let sub = a - b;
let mul = a * b;
let div = a / b;
let mod = a % b;
